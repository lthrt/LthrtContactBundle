{% extends 'LthrtContactBundle:State:state.html.twig' %}

{% block body -%}
    <h1>State Edit</h1>
    {{ form_start(form) }}
    {{ form_row(form.abbr) }}
    {{ form_row(form.name) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}

    <ul class="record_actions">
    <li>
        <a href="{{ path('state') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
