    {% extends 'LthrtContactBundle:Demographic:demographic.html.twig' %}

    {% block body -%}
    <h1>Demographic list</h1>

    <table class="records_list">
        <thead>
            <tr>
                    <th>Active</th>
                    <th>Id</th>
                    <th>Value</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for demographic in demographicCollection %}
            <tr>
                        <td>{{ demographic.active }}</td>
                        <td><a href="{{ path('demographic_show', { 'demographic': demographic.id }) }}">{{ demographic.id }}</a></td>
                        <td>{{ demographic.value }}</td>
                        <td>
                <ul>
                    <li>
                        <a href="{{ path('demographic_show', { 'demographic' : demographic.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('demographic_edit', { 'demographic' : demographic.id }) }}">edit</a>
                    </li>
                </ul>
                        </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

            <ul>
            <li>
                <a href="{{ path('demographic_new') }}">
                    Create a new entry
                </a>
            </li>
        </ul>
    {% endblock body %}
