{% extends 'LthrtContactBundle:Zip:zip.html.twig' %}

{% block body -%}
    <h1>Zip Create</h1>

    {{ form_start(form) }}
    {{ form_row(form.zip) }}
    {{ form_row(form.city) }}
    {{ form_row(form.state) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('zip') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock body %}
