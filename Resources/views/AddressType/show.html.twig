{% extends 'LthrtContactBundle:AddressType:addresstype.html.twig' %}

{% block body -%}
    <h1>AddressType</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Active</th>
                <td>{{ addresstype.active }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ addresstype.created|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ addresstype.updated|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ addresstype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ addresstype.name }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('addresstype_list') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('addresstype_edit', { 'addresstype' : addresstype.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock body %}
