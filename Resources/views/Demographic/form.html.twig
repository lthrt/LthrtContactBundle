{{ form_start(form) }}
{{ form_row(form.active) }}
{{ form_row(form.value) }}
{{ form_row(form.type) }}
<input type="submit" value="{{ submitLabel }}" class="btn btn-default pull-right" />
{{ form_rest(form) }}
{{ form_end(form) }}
