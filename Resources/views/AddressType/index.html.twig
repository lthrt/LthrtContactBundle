{% extends 'LthrtContactBundle:AddressType:addresstype.html.twig' %}

{% block body -%}
    <h1>AddressType list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for addresstype in addresstypeCollection %}
            <tr>
                <td><a href="{{ path('addresstype_show', { 'addresstype': addresstype.id }) }}">{{ addresstype.name }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('addresstype_show', { 'addresstype' : addresstype.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('addresstype_edit', { 'addresstype' : addresstype.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('addresstype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock body %}
