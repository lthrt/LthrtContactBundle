{% extends 'LthrtContactBundle:Address:address.html.twig' %}

{% block contentbody -%}
    <h1>Address Edit</h1>
    {% include 'LthrtContactBundle:Address:form.html.twig' with { 'submitLabel' : 'Update' } %}
    <ul class="record_actions">
    <li>
        <a href="{{ path('address_list') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock contentbody %}
