{% extends 'LthrtContactBundle:Person:person.html.twig' %}
{% block body -%}
    <h1>Create Person</h1>
    {% include 'LthrtContactBundle:Person:form.html.twig' with { 'submitLabel' : 'Create' } %}
        <ul class="record_actions">
    <li>
        <a href="{{ path('person_list') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock body %}