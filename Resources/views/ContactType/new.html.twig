{% extends 'LthrtContactBundle:ContactType:contacttype.html.twig' %}

{% block body -%}
    <h1>ContactType Create</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contacttype') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
